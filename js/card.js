// Логика для страницы карты
document.addEventListener('DOMContentLoaded', () => {
    const card = document.getElementById('myCard');

    card.addEventListener('click', () => {
        alert('Номер карты скопирован (имитация)');
        // Здесь в будущем будет реальный API для копирования
    });
});